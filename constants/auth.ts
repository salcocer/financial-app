// Placeholder brand — the project has no configured brand name yet (app.json
// still has the default Expo template values). Rename here to update it everywhere.
export const APP_NAME = 'Finly';
export const APP_TAGLINE = 'SUBSCRIPTION TRACKER';

// Must match the Clerk Dashboard password policy (User & authentication > Password).
export const PASSWORD_MIN_LENGTH = 15;

export const RESEND_COOLDOWN_SECONDS = 30;
